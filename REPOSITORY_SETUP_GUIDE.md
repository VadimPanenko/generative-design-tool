# Инструкция по настройке репозитория и загрузке домашнего задания №1

Данная инструкция содержит все шаги от создания репозитория до полной подготовки домашней работы для сдачи.

---

## Шаг 1: Создание репозитория на GitHub

### 1.1 Создайте новый репозиторий

1. Перейдите на [GitHub](https://github.com)
2. Нажмите на кнопку **"New"** (или "+" → "New repository")
3. Заполните информацию:
   - **Repository name**: `mlsd-generative-service` (или любое другое название, отражающее ваш проект)
   - **Description**: `ML System Design: Генеративный сервис для дизайнеров`
   - **Visibility**: 
     - ✅ **Public** (если требуется для курса)
     - или **Private** (если планируете давать доступ только ментору)
   - **Initialize repository**:
     - ❌ НЕ добавляйте README.md (мы добавим свой)
     - ❌ НЕ добавляйте .gitignore (добавим свой)
     - ❌ НЕ выбирайте лицензию на этом этапе

4. Нажмите **"Create repository"**

### 1.2 Запомните URL репозитория

После создания GitHub покажет URL вашего репозитория:
```
https://github.com/your-username/mlsd-generative-service.git
```

---

## Шаг 2: Инициализация локального репозитория

### 2.1 Откройте терминал в папке hw_1

```bash
# Перейдите в папку с домашним заданием
cd C:\Users\vadim\Desktop\MlSys\hw_1
```

### 2.2 Инициализируйте Git репозиторий

```bash
# Инициализация Git
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Setup project structure"
```

### 2.3 Свяжите с удаленным репозиторием

```bash
# Добавьте удаленный репозиторий (замените URL на ваш)
git remote add origin https://github.com/your-username/mlsd-generative-service.git

# Проверьте, что remote добавлен
git remote -v
```

### 2.4 Отправьте код в main ветку

```bash
# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

---

## Шаг 3: Создание ветки для домашней работы

Согласно требованиям курса, каждая домашняя работа должна быть в отдельной ветке **hw_n**.

### 3.1 Создайте и переключитесь на ветку hw_1

```bash
# Создайте новую ветку hw_1 и переключитесь на нее
git checkout -b hw_1

# Убедитесь, что вы на нужной ветке
git branch
```

### 3.2 Отправьте ветку на GitHub

```bash
# Отправьте ветку hw_1 на GitHub
git push -u origin hw_1
```

---

## Шаг 4: Добавление .gitignore файла

Создайте файл `.gitignore` для исключения ненужных файлов из репозитория.

### 4.1 Создайте файл .gitignore

Создайте файл `.gitignore` в корне проекта со следующим содержимым:

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/

# PyCharm
.idea/

# VS Code
.vscode/

# Jupyter Notebook
.ipynb_checkpoints

# Environment variables
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Model weights (if large)
*.safetensors
*.bin
*.pth
*.ckpt

# Generated images (if stored locally)
# generated_images/

# Pre-commit
.pre-commit-config.yaml.bak
```

### 4.2 Добавьте .gitignore в репозиторий

```bash
git add .gitignore
git commit -m "Add .gitignore file"
git push origin hw_1
```

---

## Шаг 5: Настройка pre-commit хуков

Pre-commit хуки будут автоматически проверять код перед каждым коммитом.

### 5.1 Установите pre-commit

```bash
# Установите pre-commit (если еще не установлен)
pip install pre-commit

# Или используйте pipx для глобальной установки
pipx install pre-commit
```

### 5.2 Установите хуки в репозиторий

```bash
# Активируйте pre-commit хуки
pre-commit install

# (Опционально) Запустите на всех файлах для проверки
pre-commit run --all-files
```

После этого pre-commit будет автоматически запускаться перед каждым `git commit`.

---

## Шаг 6: Создание Pull Request (PR)

### 6.1 Перейдите на GitHub

1. Откройте ваш репозиторий на GitHub: `https://github.com/your-username/mlsd-generative-service`
2. Вы увидите уведомление о недавно отправленной ветке `hw_1`
3. Нажмите **"Compare & pull request"**

### 6.2 Заполните информацию о PR

- **Title**: `Домашнее задание №1 - ML System Design Doc`
- **Description**:
  ```markdown
  ## Выполненные задачи
  
  ### Сетап репозитория (3 балла)
  - [x] Настроены линтеры и форматеры (ruff) - 1 балл
  - [x] Настроен pre-commit - 1 балл
  - [x] Заполнен README.md с информацией о проекте - 1 балл
  
  ### Дизайн-документ (7 баллов)
  - [x] Зачем идем в разработку продукта - 1 балл
  - [x] Бизнес-требования и ограничения - 2 балла
  - [x] Скоуп проекта/итерации - 1 балл
  - [x] Предпосылки решения - 1 балл
  - [x] Постановка задачи - 1 балл
  - [x] Блок-схема решения - 1 балл
  
  ## Структура репозитория
  
  ```
  .
  ├── docs/
  │   └── ml_system_design_doc.md    # Полный дизайн-документ ML системы
  ├── .pre-commit-config.yaml        # Конфигурация pre-commit
  ├── pyproject.toml                 # Настройки ruff
  ├── .gitignore                     # Исключения для Git
  └── README.md                      # Описание проекта
  ```
  
  ## Ссылки
  
  - Дизайн-документ: [docs/ml_system_design_doc.md](docs/ml_system_design_doc.md)
  - README: [README.md](README.md)
  
  ## Проект
  
  Генеративный сервис для автоматизации создания графических материалов для дизайнеров.
  Использует FLUX.1-dev и LLM для генерации 3D-иконок в корпоративном стиле.
  ```

- **Base branch**: `main`
- **Compare branch**: `hw_1`

### 6.3 Создайте PR

Нажмите **"Create pull request"**

---

## Шаг 7: Добавление ментора и ревьюверов

### 7.1 Добавьте ментора в Reviewers

1. В открытом PR справа найдите секцию **"Reviewers"**
2. Нажмите на иконку настроек (шестеренка)
3. Введите GitHub username вашего ментора
4. Выберите его из списка

### 7.2 Добавьте кросс-ревьюверов

Когда будут назначены студенты для кросс-ревью:

1. Повторите шаг 7.1 для каждого студента-ревьювера
2. Обычно назначается 2 студента

### 7.3 Дайте доступ ментору к репозиторию (если Private)

Если ваш репозиторий **Private**:

1. Перейдите в **Settings** репозитория
2. Выберите **Collaborators** в левом меню
3. Нажмите **"Add people"**
4. Введите GitHub username ментора
5. Выберите уровень доступа: **Write** (для комментариев и одобрения)
6. Отправьте приглашение

---

## Шаг 8: Проверка перед сдачей

### 8.1 Чек-лист готовности

Убедитесь, что выполнены все требования:

#### Сетап репозитория:
- ✅ Настроен ruff (файл `pyproject.toml` присутствует)
- ✅ Настроен pre-commit (файл `.pre-commit-config.yaml` присутствует)
- ✅ Заполнен README.md с информацией о проекте

#### Дизайн-документ:
- ✅ Создана папка `docs/`
- ✅ Создан файл `ml_system_design_doc.md`
- ✅ Заполнены все обязательные разделы:
  - Зачем идем в разработку продукта
  - Бизнес-требования и ограничения
  - Скоуп проекта/итерации
  - Предпосылки решения
  - Постановка задачи
  - Блок-схема решения
  - Этапы решения задачи

#### Git и GitHub:
- ✅ Код залит в ветку `hw_1`
- ✅ Создан Pull Request в ветку `main`
- ✅ Добавлен ментор в Reviewers
- ✅ Добавлены кросс-ревьюверы (когда назначат)

### 8.2 Тестирование pre-commit

Перед финальным push проверьте, что pre-commit работает:

```bash
# Запустите pre-commit на всех файлах
pre-commit run --all-files

# Если есть ошибки, исправьте их и закоммитьте
git add .
git commit -m "Fix linting issues"
git push origin hw_1
```

---

## Шаг 9: Дедлайны и следующие шаги

### 9.1 Важные даты

- **Жесткий дедлайн сдачи**: 19.11.2025, 20:00 МСК
- **Дедлайн кросс-ревью**: 27.11.2025, 20:00 МСК

### 9.2 После создания PR

1. **Дождитесь ревью** от студентов и ментора
2. **Если есть комментарии** - внесите исправления:
   ```bash
   # Внесите изменения в файлы
   git add .
   git commit -m "Fix: address review comments"
   git push origin hw_1
   ```
3. **Если все ок** - ментор одобрит PR
4. **Мерж в main** - после всех одобрений сделайте merge

### 9.3 Кросс-ревью других работ

**Не забудьте!** Вам нужно сделать ревью двух работ других студентов до **27.11.2025, 20:00 МСК**. 

Без выполнения кросс-ревью ваши баллы за текущую работу не будут выставлены!

---

## Шаг 10: Полезные команды Git

### Основные команды

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Посмотреть историю коммитов
git log --oneline

# Отменить изменения в файле (до add)
git checkout -- <file>

# Отменить последний коммит (изменения останутся)
git reset --soft HEAD~1

# Обновить локальную ветку из удаленной
git pull origin hw_1

# Посмотреть все ветки
git branch -a

# Переключиться на другую ветку
git checkout <branch-name>
```

### Работа с pre-commit

```bash
# Установить хуки
pre-commit install

# Запустить на всех файлах
pre-commit run --all-files

# Обновить версии хуков
pre-commit autoupdate

# Пропустить хуки для конкретного коммита (НЕ РЕКОМЕНДУЕТСЯ)
git commit --no-verify -m "message"
```

### Работа с ruff

```bash
# Проверить код
ruff check .

# Автоматически исправить проблемы
ruff check --fix .

# Форматировать код
ruff format .

# Проверить конкретный файл
ruff check path/to/file.py
```

---

## Troubleshooting (Решение проблем)

### Проблема 1: Pre-commit не запускается

**Решение:**
```bash
# Переустановите хуки
pre-commit uninstall
pre-commit install
```

### Проблема 2: Ruff выдает ошибки

**Решение:**
```bash
# Попробуйте автоматическое исправление
ruff check --fix .

# Если не помогает, смотрите конкретные ошибки:
ruff check .
```

### Проблема 3: Конфликты при push

**Решение:**
```bash
# Обновите локальную ветку
git pull origin hw_1

# Разрешите конфликты вручную
# Затем закоммитьте
git add .
git commit -m "Resolve conflicts"
git push origin hw_1
```

### Проблема 4: Не могу создать PR

**Причины:**
- Возможно, вы не отправили ветку на GitHub (`git push origin hw_1`)
- Убедитесь, что вы на правильной ветке (`git branch`)

### Проблема 5: Ментор не видит репозиторий

**Решение:**
- Если репозиторий Private, добавьте ментора в Collaborators (см. Шаг 7.3)
- Проверьте, что отправили приглашение

---

## Дополнительные материалы

### Полезные ссылки

- [Git документация](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Pre-commit документация](https://pre-commit.com/)
- [Ruff документация](https://docs.astral.sh/ruff/)
- [Markdown Guide](https://www.markdownguide.org/)

### Рекомендации по Git workflow

1. **Делайте частые коммиты** с понятными сообщениями
2. **Используйте осмысленные названия** для коммитов
3. **Не коммитьте большие бинарные файлы** (модели, изображения) - используйте Git LFS или внешнее хранилище
4. **Проверяйте код перед push** - используйте pre-commit и ruff
5. **Обновляйте документацию** вместе с кодом

### Формат коммит-сообщений

Рекомендуемый формат:

```
<type>: <краткое описание>

<детальное описание (опционально)>

<footer (опционально)>
```

Типы:
- `feat`: Новая функциональность
- `fix`: Исправление бага
- `docs`: Изменения в документации
- `style`: Форматирование кода
- `refactor`: Рефакторинг
- `test`: Добавление тестов
- `chore`: Рутинные задачи (обновление зависимостей, etc.)

Примеры:
```bash
git commit -m "feat: Add prompt enrichment service"
git commit -m "docs: Update ML system design doc"
git commit -m "fix: Resolve memory leak in image generation"
```

---

## Итоговый чек-лист перед сдачей

### Репозиторий
- [ ] Репозиторий создан на GitHub
- [ ] Все файлы загружены в ветку `hw_1`
- [ ] .gitignore настроен корректно
- [ ] Нет больших бинарных файлов в репозитории

### Код и конфигурация
- [ ] pyproject.toml с настройками ruff присутствует
- [ ] .pre-commit-config.yaml присутствует
- [ ] Pre-commit хуки установлены и работают
- [ ] Все файлы проходят проверку ruff

### Документация
- [ ] README.md заполнен и содержит полную информацию о проекте
- [ ] docs/ml_system_design_doc.md создан
- [ ] Все обязательные разделы дизайн-документа заполнены
- [ ] Блок-схема решения присутствует

### GitHub
- [ ] Pull Request создан из ветки `hw_1` в `main`
- [ ] Описание PR содержит информацию о выполненных задачах
- [ ] Ментор добавлен в Reviewers
- [ ] Кросс-ревьюверы добавлены (когда назначат)
- [ ] Ментору дан доступ к репозиторию (если Private)

### Дедлайны
- [ ] Работа сдана до 19.11.2025, 20:00 МСК
- [ ] Готовность к выполнению кросс-ревью до 27.11.2025, 20:00 МСК

---

## Контакты и поддержка

Если у вас возникли вопросы:

1. **Технические вопросы по Git/GitHub** - поищите в [GitHub Docs](https://docs.github.com/)
2. **Вопросы по заданию** - напишите ментору или в общий чат курса
3. **Проблемы с инструментами** - проверьте документацию [Ruff](https://docs.astral.sh/ruff/) или [Pre-commit](https://pre-commit.com/)

---

**Удачи с выполнением домашнего задания!** 🚀

---

*Документ подготовлен: Ноябрь 2025*  
*Версия: 1.0*

