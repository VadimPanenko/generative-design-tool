# ML System Design Doc
## Генеративный сервис для дизайнеров - MVP

*Дизайн-документ разработан на основе шаблона от [Reliable ML](https://github.com/IrinaGoloshchapova/ml_system_design_doc_ru)*

---

## 1. Цели и предпосылки

### 1.1. Зачем идем в разработку продукта?

**Бизнес-цель:**

Создать AI-сервис для автоматической генерации иконок и графических материалов в корпоративном стиле, который:
- Сокращает время создания визуального контента с 2-3 часов до 20-40 минут
- Автоматически применяет корпоративный стиль и цвета из бренд-бука
- Снижает зависимость от внешних подрядчиков и специалистов 3D-моделирования

**Почему станет лучше от использования ML:**

| Аспект | Как сейчас | Как будет с AI-сервисом |
|--------|-----------|------------------------|
| Время создания | 2-3 часа ручной работы в 3D-редакторах | 1-4 минуты автоматической генерации |
| Навыки | Требуются навыки 3D-моделирования | Простое текстовое описание на русском |
| Стиль | Ручное согласование с бренд-буком | Автоматическое применение корп. стиля |
| Итерации | Множественные согласования | Быстрые правки через inpainting |
| Подрядчики | Высокие затраты на специалистов | Внутренний инструмент для типовых задач |

**Критерии успеха итерации:**

- Сокращение времени создания графики минимум на **70%**
- Положительная обратная связь от **60%+** дизайнеров
- Использование сервиса для **30%+** новых задач по созданию 3D-иконок
- Соответствие бренд-буку в **70%+** случаев без ручной доработки
- Снижение итераций согласования на **30%**

---

### 1.2. Бизнес-требования и ограничения

**Функциональные требования:**
- 3D изображения в PNG формате без фона, в корпоративной стилистике
- Генерация предметов, 3D значков, ассетов для интерфейсов и презентаций
- Время генерации 1024x1024px не более 4 минут
- Простой веб-интерфейс с промптами на русском языке
- Автоматическое применение корпоративных цветов

**Бизнес-ограничения:**
- Использование внутренних GPU-серверов с ограниченными ресурсами
- Все данные хранятся внутри корпоративной инфраструктуры
- Ограниченный бюджет на внешние вычислительные ресурсы

**Описание бизнес-процесса пилота:**

1. **Фаза тестирования** (1-2 месяца): 5-7 дизайнеров тестируют параллельно с традиционными методами
2. **Фаза расширения** (2-3 месяца): Доступ для всей дизайн-команды (15-20 человек)
3. **Оценка результатов**: Сравнение времени, анализ удовлетворенности, подсчет экономического эффекта

**Критерии успешного пилота:**
- Adoption rate ≥ 60% (регулярное использование)
- Качество: ≥ 70% соответствие бренд-буку
- NPS ≥ 40
- Экономия ~10 часов/неделю для пилотной группы

---

### 1.3. Что входит в скоуп проекта/итерации

**Входит в скоуп MVP:**

*Функциональность:*
- Генерация 3D-иконок по текстовому описанию
- Автоматическое обогащение промптов с учетом корпоративных гайдлайнов
- Применение корпоративных цветов
- Веб-интерфейс с базовым функционалом
- Dual prompting (T5 + CLIP)

*Технические аспекты:*
- Развертывание на GPU-серверах (A40)
- Интеграция FLUX.1-dev для генерации
- Интеграция DeepSeek-R1 / Qwen2.5 для обогащения промптов
- Документация разработки

**НЕ входит в скоуп MVP:**
- Inpainting, upscaling, удаление фона
- Хранение истории промптов
- Плагин для Figma
- Генерация русскоязычного текста на изображениях
- Файн-тюнинг моделей на корпоративных данных
- RAG-система для промптов
- API для интеграции с другими системами
- Batch-генерация

---

### 1.4. Предпосылки решения

**Выбор блоков данных:**

1. **Корпоративные гайдлайны**: Цветовая палитра, стилистические требования, примеры 3D-иконок
   - *Обоснование*: Необходимы для автоматического применения корпоративного стиля

2. **История промптов и изображений**: Промпты пользователей, сгенерированные изображения, оценки качества
   - *Обоснование*: Анализ эффективности, накопление базы успешных промптов

3. **Обратная связь**: Оценки соответствия бренд-буку, комментарии, метрики использования
   - *Обоснование*: Критична для итеративного улучшения системы

**Технологический стек:**

| Компонент | Решение | Обоснование |
|-----------|---------|-------------|
| Генеративная модель | FLUX.1-dev | State-of-the-art качество, хорошее понимание промптов, возможность кастомизации |
| LLM для промптов | DeepSeek-R1 / Qwen2.5 | Поддержка русского языка, быстрая обработка |
| Инфраструктура | GPU NVIDIA A40 (48GB) | Требования безопасности, достаточные ресурсы |
| Backend/Frontend | FastAPI + веб-интерфейс | Быстрая разработка, легкая интеграция с ML |

**Гранулярность и горизонт:**
- Real-time генерация по запросу
- Одно изображение за один промпт
- Персонализация на уровне пользователя

**Предположения о поведении пользователей:**
- Формулировка запросов на русском в свободной форме
- 3-5 генераций за сессию с итеративными улучшениями
- Основной use-case: отдельные иконки, не сложные сцены

---

## 2. Методология

### 2.1. Постановка задачи

**Техническая формулировка:**

Разработка **генеративной системы text-to-image** с автоматическим стиль-адаптером для создания графических материалов в корпоративном стиле.

**Компоненты задачи:**

1. **Text-to-Image генерация**: Преобразование текста на русском в изображения
2. **Автоматическое обогащение промптов**: NLP-обработка через LLM, добавление стилистических параметров, перевод на английский, генерация negative prompts
3. **Оркестрация пайплайна**: Управление последовательностью вызовов моделей, обработка результатов

**Типы задач:**
- Основная: Условная генерация изображений (Conditional Image Generation)
- Вспомогательная: Улучшение текста (Text Enhancement)

**Метрики оценки:**
- **Технические**: Время генерации, использование GPU, throughput
- **Качественные**: CLIP score, соответствие бренд-буку (экспертная оценка)
- **Бизнес**: Процент использования, удовлетворенность, экономия времени

---

### 2.2. Схема решения

<img src="https://raw.githubusercontent.com/VadimPanenko/generative-design-tool/refs/heads/main/docs/generative_service_scheme.png" alt="generative_service_scheme.png">

---

### 2.3. Этапы решения задачи

---

## Этап 1. Подготовка данных

### Цель этапа
Обеспечить наличие всех необходимых данных для работы системы и провести их качественный анализ.

### Описание данных и сущностей

> **Примечание:** Ниже подробно описаны все типы данных, используемые в системе, с указанием структуры экземпляров и способов использования.

---

#### 1. Тестовые промпты пользователей

**Описание:** Текстовые запросы на русском языке, описывающие желаемое изображение.

**Источник:** Собраны во время интервью с дизайнерами (5 встреч), а также синтезированы на основе анализа типовых задач дизайн-отдела.

**Цель сбора и анализа:** 
- Понять, как дизайнеры формулируют запросы в естественной форме
- Выявить паттерны (короткие/длинные, наличие технических терминов)
- Определить необходимость системы обогащения промптов
- Сформировать тестовый набор для оценки качества генерации

**Структура экземпляра данных:**

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `prompt_id` | string | Уникальный идентификатор | "p_001" |
| `text` | string | Текст промпта на русском | "красивая иконка автомобиля" |
| `category` | string | Категория объекта | "техника", "UI", "бизнес" |
| `complexity` | enum | Сложность запроса | "простой", "средний", "сложный" |
| `source` | string | Источник промпта | "интервью", "синтетический" |
| `has_style_hint` | boolean | Есть ли указание на стиль | false |
| `word_count` | int | Количество слов | 4 |
| `char_count` | int | Количество символов | 28 |

**Пример записи:**
```json
{
  "prompt_id": "p_015",
  "text": "3D иконка смартфона в изометрии для мобильного приложения",
  "category": "UI",
  "complexity": "средний",
  "source": "интервью",
  "has_style_hint": true,
  "word_count": 8,
  "char_count": 54
}
```

**Результаты анализа промптов:**
- Средняя длина: ~35 символов (короткие, неинформативные)
- Только 6% содержат явное указание стиля
- Преобладающие категории: UI-элементы (22%), техника (18%), бизнес (16%)
- **Вывод:** Необходима система обогащения промптов через LLM

---

#### 2. Корпоративный бренд-бук

**Описание:** Документ с правилами оформления визуальных материалов в корпоративном стиле.

**Источник:** Внутренняя документация компании (Figma, PDF).

**Цель использования:** Извлечение правил для автоматического добавления стилистических параметров в промпты.

**Структура данных из бренд-бука:**

| Компонент | Формат | Количество | Использование |
|-----------|--------|------------|---------------|
| Цветовая палитра | HEX-коды | 8 основных цветов | Инъекция в промпты |
| Типографика | Названия шрифтов | 3 семейства | Справочно |
| Примеры 3D-графики | Изображения (описания) | ~15 примеров | Анализ стиля |
| Правила композиции | Текстовые описания | ~10 правил | Системный промпт LLM |
| Запрещённые практики | Текстовые описания | ~8 пунктов | Negative prompts |

**Формализованная цветовая палитра:**

| Название | HEX-код | Использование |
|----------|---------|---------------|
| Primary Blue | #005BFF | Основной акцент |
| Light Blue | #00A0FF | Вторичные элементы |
| Accent Pink | #FF1493 | Акценты |
| White | #FFFFFF | Фон, контраст |
| Dark | #1A1A1A | Тени |
| ... | ... | ... |

---

#### 3. Примеры 3D-иконок (референсы)

**Описание:** Существующие 3D-иконки, созданные дизайнерами вручную, соответствующие корпоративному стилю.

**Источник:** Библиотека дизайн-компонентов в Figma.

**Цель использования:**
- Экспертная оценка качества генерации (сравнение с референсами)
- Анализ стилистических особенностей (освещение, материалы, углы)
- Валидация соответствия бренд-буку

**Структура экземпляра данных:**

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `image_id` | string | Уникальный идентификатор | "ref_042" |
| `file_path` | string | Путь к файлу | "icons/3d/smartphone.png" |
| `category` | string | Категория объекта | "техника" |
| `object_name` | string | Название объекта | "смартфон" |
| `resolution` | string | Разрешение | "1024x1024" |
| `has_transparency` | boolean | Прозрачный фон | true |
| `style_tags` | list[string] | Стилевые теги | ["isometric", "soft_shadows"] |
| `creation_date` | date | Дата создания | "2024-03-15" |
| `author` | string | Автор | "designer_a" |

**Статистика по референсам:**
- Общее количество: ~200 изображений
- Форматы: PNG (95%), SVG (5%)
- Разрешения: 1024x1024 (80%), 512x512 (15%), другие (5%)
- Все с прозрачным фоном

**Важно:** Этот объём (200 изображений) недостаточен для fine-tuning модели (требуется 1000+), поэтому используется как validation set для экспертной оценки.

---

#### 4. Предобученная модель FLUX.1-dev

**Описание:** Диффузионная модель для генерации изображений по текстовым описаниям.

**Источник:** HuggingFace (black-forest-labs/FLUX.1-dev)

**Структура файлов модели:**

| Файл | Размер | Назначение |
|------|--------|------------|
| flux1-dev.safetensors | 23.8 GB | Основные веса модели |
| ae.safetensors | 335 MB | Variational Autoencoder |
| t5xxl_fp16.safetensors | ~10 GB | T5 Text Encoder |
| clip_l.safetensors | ~250 MB | CLIP Text Encoder |
| config.json | 2 KB | Конфигурация |

**Параметры модели:**
- Архитектура: Rectified Flow Transformer
- Размер: ~12B параметров
- Поддерживаемые разрешения: до 4096x4096
- Dual text encoding: T5 + CLIP

---

#### 5. LLM модели для обогащения промптов

**Описание:** Языковые модели для преобразования пользовательских промптов.

**Модели:**

| Модель | Назначение | Размер | Квантизация |
|--------|-----------|--------|-------------|
| DeepSeek-R1-671B-AWQ | Сложные задачи обогащения | 671B → ~200GB | AWQ 4-bit |
| Qwen2.5-14B-Instruct | Быстрые операции (перевод, negative) | 14B → ~10GB | FP16 |

**Системные промпты (примеры):**

```
# Для обогащения промптов
You are an expert prompt engineer for FLUX.1 image generation.
Transform the user's brief description into a detailed prompt:
1. Add "3D render", "isometric view", "soft lighting"
2. Include corporate colors: #005BFF, #00A0FF
3. Keep the core object from original prompt
4. Output in English only
```

---

#### 6. Данные обратной связи (планируемые к сбору)

**Описание:** Оценки качества генерации от пользователей и экспертов.

**Структура экземпляра:**

| Поле | Тип | Описание |
|------|-----|----------|
| `feedback_id` | string | ID записи |
| `image_id` | string | ID сгенерированного изображения |
| `user_id` | string | ID пользователя (псевдонимизирован) |
| `usability` | boolean | Можно использовать без доработки (да/нет) |
| `brand_compliance` | int (1-5) | Соответствие бренд-буку |
| `style_quality` | int (1-5) | Качество стиля |
| `comment` | string | Текстовый комментарий |
| `timestamp` | datetime | Время оценки |

---

### Сводная таблица данных

| Название данных | Источник | Формат | Объем | Проверено качество | Назначение |
|-----------------|----------|--------|-------|-------------------|------------|
| Тестовые промпты | Интервью с дизайнерами | JSON/текст | 50+ записей | ✓ | Тестирование пайплайна, анализ паттернов |
| Корпоративный бренд-бук | Figma, внутренние документы | PDF, Figma | ~50 страниц | ✓ | Извлечение правил стиля |
| Цветовая палитра | Бренд-бук | HEX-коды | 8 цветов | ✓ | Инъекция в промпты |
| Примеры 3D-иконок | Библиотека Figma | PNG, SVG | ~200 файлов | ✓ | Validation set, экспертная оценка |
| FLUX.1-dev | HuggingFace | safetensors | 23.8 GB | ✓ | Генерация изображений |
| LLM модели | Внутренний Model Hub | Квантизированные | ~14-200B параметров | ✓ | Обогащение промптов |

---

### Процесс генерации и обработки данных

> **Примечание:** Описание полного пути данных от пользовательского ввода до финального изображения.

#### Схема потока данных

```
┌───────────────────────────────────────────────────────────────────────────────┐
│                          DATA FLOW PIPELINE                                   │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ВХОД                    ОБРАБОТКА                         ВЫХОД              │
│  ─────                   ─────────                         ─────              │
│                                                                               │
│  ┌─────────────┐    ┌──────────────────────────────────┐    ┌─────────────┐   │
│  │ User Input  │    │        STEP 1: Валидация         │    │   Отказ     │   │
│  │ "иконка     │───▶│  - UTF-8 encoding check         │───▶│   (если     │   │
│  │  машины"    │    │  - Length: 1-500 символов        │    │   ошибка)   │   │
│  └─────────────┘    │  - Content moderation            │    └─────────────┘   │
│                     └──────────────┬───────────────────┘                      │
│                                    │ ✓ Валидный промпт                        │
│                                    ▼                                          │
│                     ┌──────────────────────────────────┐                      │
│                     │        STEP 2: LLM Enrichment    │                      │
│                     │  Вход: "иконка машины"           │                      │
│                     │  ─────────────────────────────── │                      │
│                     │  1. Определение категории        │                      │
│                     │  2. Добавление техн. параметров  │                      │
│                     │  3. Инъекция корп. цветов        │                      │
│                     │  4. Перевод на английский        │                      │
│                     │  5. Генерация negative prompt    │                      │
│                     │  ─────────────────────────────── │                      │
│                     │  Выход: JSON с 3 промптами       │                      │
│                     └──────────────┬───────────────────┘                      │
│                                    │                                          │
│                                    ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │  Промежуточные данные (EnrichedPrompt):                                 │  │
│  │  {                                                                      │  │
│  │    "prompt_t5": "A stunning 3D render of a sleek modern car icon,       │  │
│  │                  isometric view, soft ambient lighting, vibrant         │  │
│  │                  primary blue #005BFF and light blue #00A0FF colors,│  │
│  │                  minimalist design, transparent background, ...",       │  │
│  │    "prompt_clip": "3D car icon, isometric, blue, minimal, clean",       │  │
│  │    "negative_prompt": "text, watermark, blurry, low quality, ..."       │  │
│  │  }                                                                      │  │
│  └─────────────────────────────────┬───────────────────────────────────────┘  │
│                                    │                                          │
│                                    ▼                                          │
│                     ┌──────────────────────────────────┐                      │
│                     │        STEP 3: Tokenization      │                      │
│                     │  ─────────────────────────────── │                      │
│                     │  T5 Encoder:                     │                      │
│                     │   - Max 512 tokens               │                      │
│                     │   - Детальное описание           │                      │
│                     │   - Выход: [1, 512, 4096] tensor │                      │
│                     │                                  │                      │
│                     │  CLIP Encoder:                   │                      │
│                     │   - Max 77 tokens                │                      │
│                     │   - Визуальные ключевые слова    │                      │
│                     │   - Выход: [1, 77, 768] tensor   │                      │
│                     └──────────────┬───────────────────┘                      │
│                                    │                                          │
│                                    ▼                                          │
│                     ┌──────────────────────────────────┐                      │
│                     │     STEP 4: Diffusion Process    │                      │
│                     │  ─────────────────────────────── │                      │
│                     │  Параметры:                      │                      │
│                     │   - num_inference_steps: 28      │                      │
│                     │   - guidance_scale: 3.5          │                      │
│                     │   - height: 1024, width: 1024    │                      │
│                     │  ─────────────────────────────── │                      │
│                     │  Итеративная денойзинг:          │                      │
│                     │   Noise → ... → Image            │                      │
│                     │   (28 шагов, ~1 мин)             │                      │
│                     └──────────────┬───────────────────┘                      │
│                                    │                                          │
│                                    ▼                                          │
│                     ┌──────────────────────────────────┐    ┌─────────────┐   │
│                     │        STEP 5: Post-processing   │    │   OUTPUT    │   │
│                     │  - VAE decoding                  │───▶│  PNG Image │   │
│                     │  - RGBA conversion               │    │  1024x1024  │   │
│                     │  - Quality check                 │    │  ~2-5 MB    │   │
│                     └──────────────────────────────────┘    └─────────────┘   │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘
```

#### Детальное описание каждого шага

**STEP 1: Валидация входных данных**

| Проверка | Критерий | Действие при ошибке |
|----------|----------|---------------------|
| Кодировка | UTF-8 | Попытка конвертации, иначе отказ |
| Длина | 1-500 символов | Отказ с сообщением |
| Запрещённый контент | Черный список слов | Отказ с модерацией |
| Пустой ввод | len(text.strip()) > 0 | Отказ |

**STEP 2: Обогащение через LLM**

Входные данные:
- Пользовательский промпт (русский текст)
- Системный промпт с правилами обогащения
- Цветовая палитра (8 HEX-кодов)

Выходные данные:
```json
{
  "prompt_t5": "Детальное описание на английском (до 200 слов)",
  "prompt_clip": "Ключевые слова через запятую (до 20 слов)",
  "negative_prompt": "Список нежелательных элементов"
}
```

**STEP 3: Токенизация**

| Encoder | Макс. токенов | Тип эмбеддинга | Размерность |
|---------|---------------|----------------|-------------|
| T5-XXL | 512 | Детальное описание | [1, 512, 4096] |
| CLIP-L | 77 | Визуальные концепты | [1, 77, 768] |

**STEP 4: Диффузионный процесс**

Параметры генерации:
- `num_inference_steps=28` — количество шагов денойзинга
- `guidance_scale=3.5` — баланс между следованием промпту и качеством
- Scheduler: FlowMatchEulerDiscreteScheduler

**STEP 5: Пост-обработка**

- VAE декодирование латентного пространства в пиксели
- Конвертация в RGBA (с альфа-каналом для прозрачности)
- Сохранение в PNG формате

---

### Решение проблемы недостатка данных

#### Проблема с изображениями

**Текущая ситуация:**
- Имеется ~200 референсных 3D-иконок в корпоративном стиле
- Для полноценного fine-tuning требуется 1000-5000+ изображений
- Ручное создание такого объёма экономически нецелесообразно (200+ часов работы дизайнеров)

**Стратегия решения (поэтапная):**

| Этап | Подход | Результат |
|------|--------|-----------|
| **MVP (сейчас)** | Использование предобученной модели без fine-tuning | 70-80% соответствие стилю через промпт-инженеринг |
| **Месяцы 1-3** | Накопление данных через использование сервиса | +500 изображений с оценками |
| **Месяцы 3-6** | Фильтрация качественных генераций (оценка ≥4) | ~300 отобранных изображений |
| **Месяцы 6-12** | LoRA fine-tuning на накопленном датасете | 85-90% соответствие стилю |

**Обоснование выбора подхода без fine-tuning для MVP:**

1. **Качество предобученной модели:** FLUX.1-dev обучена на миллиардах изображений и хорошо понимает концепции "3D render", "isometric", "corporate style"

2. **Эффективность промпт-инженеринга:** 
   - Тесты показали 75% usability без fine-tuning
   - Инъекция HEX-кодов цветов работает стабильно
   - LLM-обогащение добавляет недостающий контекст

3. **Экономическая эффективность:**
   - Fine-tuning: 100+ GPU-часов, риск overfitting
   - Промпт-инженеринг: 0 дополнительных затрат на обучение

4. **Быстрый запуск MVP:**
   - Fine-tuning задержит запуск на 2-3 месяца
   - Текущий подход позволяет начать пилот немедленно

---

### Конфиденциальность данных

**Анализ конфиденциальности:**

| Тип данных | Содержит ПДн | Требует обработки | Меры защиты |
|------------|--------------|-------------------|-------------|
| Промпты | Нет | Нет | Хранение во внутреннем контуре |
| Изображения | Нет | Нет | Хранение во внутреннем контуре |
| User ID | Да (косвенно) | Да | Псевдонимизация для аналитики |
| Логи использования | Да (косвенно) | Да | Агрегирование, retention 6 мес |

**Меры защиты:**
- Все данные хранятся внутри корпоративной инфраструктуры
- Доступ через корпоративную аутентификацию
- Нет передачи данных во внешние сервисы
- GDPR compliance: данные не покидают контур компании

---

### Необходимый результат этапа

- [x] Все модели загружены на GPU-серверы
- [x] Корпоративная палитра формализована (8 HEX-кодов)
- [x] Подготовлен набор из 50+ тестовых промптов
- [x] Проведен EDA с визуализациями и выводами
- [x] Документированы риски и способы их митигации
- [x] Описана структура всех сущностей данных

---

## Этап 2. Подготовка прогнозных моделей

### Цель этапа
Настроить и валидировать модели для генерации изображений и обогащения промптов.

### Особенность проекта: Inference-only подход

> **Важно:** В данном проекте используется подход **inference-only** — предобученные модели применяются без дополнительного обучения (fine-tuning). Это обосновано спецификой задачи.

#### Почему не обучаем модели с нуля или не делаем fine-tuning

**1. Недостаток данных для fine-tuning:**
- Имеется ~200 референсных изображений
- Для качественного fine-tuning диффузионных моделей требуется 1000-5000+ изображений одного стиля
- Риск overfitting на малом датасете

**2. Качество предобученных моделей:**
- FLUX.1-dev обучена на миллиардах изображений
- Модель уже "знает" концепции 3D-рендера, изометрии, корпоративного стиля
- Нужно только правильно "попросить" через промпт

**3. Специфика generative AI:**
- В отличие от классификации/регрессии, генеративные модели оцениваются по качеству выхода
- "Функция потерь" существует только на этапе pre-training (diffusion loss)
- На inference мы не оптимизируем loss, а оцениваем качество генерации

**4. Экономическая эффективность:**
- Fine-tuning FLUX требует 100+ GPU-часов на A100
- Промпт-инженеринг даёт 75% usability без дополнительных затрат
- MVP можно запустить немедленно

---

### Бейзлайн и MVP: Сравнение

#### Бейзлайн: FLUX.1-dev без обогащения

**Описание:**
Прямое использование пользовательского промпта без обработки.

**Конфигурация:**

```python
baseline_config = {
    "model": "FLUX.1-dev",
    "prompt": user_input,  # Без обработки
    "prompt_2": user_input,  # Дублирование
    "negative_prompt": "",  # Пустой
    "guidance_scale": 3.5,
    "num_inference_steps": 28,
    "height": 1024,
    "width": 1024
}
```

**Результаты бейзлайна:**

| Метрика | Значение | Комментарий |
|---------|----------|-------------|
| CLIP Score | 0.21 | Ниже целевого (0.25) |
| Экспертная оценка | 2.8/5 | Стиль не соответствует бренду |
| Usability | 45% | Много требует доработки |
| Время генерации | 58 сек | В пределах SLA |

**Проблемы бейзлайна:**
- Короткие промпты дают размытые результаты
- Отсутствие корпоративных цветов
- Нестабильный 3D-стиль

---

#### MVP: FLUX.1-dev + LLM-обогащение + Dual Prompting

**Описание:**
Автоматическое преобразование пользовательского промпта через LLM с добавлением корпоративного стиля.

**Конфигурация:**

```python
mvp_config = {
    "model": "FLUX.1-dev",
    "prompt": t5_enriched_prompt,      # Детальное описание от LLM
    "prompt_2": clip_optimized_prompt,  # Ключевые слова
    "negative_prompt": auto_generated_negative,
    "guidance_scale": 3.5,
    "num_inference_steps": 28,
    "height": 1024,
    "width": 1024
}
```

**Результаты MVP:**

| Метрика | Бейзлайн | MVP | Улучшение |
|---------|----------|-----|-----------|
| CLIP Score | 0.21 | 0.28 | +33% |
| Экспертная оценка | 2.8/5 | 3.85/5 | +38% |
| Usability | 45% | 75% | +67% |
| Наличие корп. цветов | 10% | 95% | +850% |

---

### ML-метрики и их обоснование

> **Примечание:** Поскольку модели не обучаются (inference-only), классические loss-функции не применимы. Используются метрики оценки качества генерации.

#### Метрики качества генерации

| Метрика | Формула/Описание | Целевое значение | Обоснование выбора |
|---------|------------------|------------------|-------------------|
| **CLIP Score** | `cosine_similarity(CLIP(image), CLIP(prompt))` | ≥ 0.25 | Стандартная метрика для text-to-image. Измеряет семантическое соответствие изображения тексту. |
| **FID** | `FrechetDistance(Inception(real), Inception(generated))` | ≤ 50 | Измеряет реалистичность и качество изображений. |
| **Экспертная оценка** | Шкала 1-5 от арт-директора | ≥ 3.5 | Автоматические метрики не улавливают соответствие бренд-буку. |
| **Usability Rate** | `count(usable) / count(total)` | ≥ 70% | Может ли дизайнер сразу использовать результат. |

---

### Необходимый результат этапа

- [x] FLUX.1-dev интегрирована и протестирована
- [x] LLM для обогащения промптов настроена
- [x] Бейзлайн оценен на тестовом наборе
- [x] MVP оценен и показывает улучшение +67% usability
- [x] Метрики качества определены и обоснованы
- [x] Документация параметров генерации

---

## 3. Подготовка пилота

### 3.1. Способ оценки пилота

#### Дизайн пилота

**Этап 1 - Контролируемое тестирование (1-2 месяца):**

| Параметр | Значение |
|----------|----------|
| Участники | 5-7 опытных дизайнеров |
| Режим | Параллельно с традиционными инструментами |
| Контроль | Еженедельные синхронизации с командой |
| Задачи | Типовые задачи по созданию 3D-иконок |

**Этап 2 - Расширенное тестирование (2-3 месяца):**

| Параметр | Значение |
|----------|----------|
| Участники | 15-20 человек (вся дизайн-команда) |
| Режим | Свободное использование |
| Контроль | Ежемесячные опросы и анализ |
| Задачи | Все задачи по 3D-графике |

#### Сбор данных во время пилота

**Автоматические метрики (собираются системой):**

| Метрика | Способ сбора | Частота |
|---------|--------------|---------|
| Количество генераций | Логи API | Real-time |
| Уникальные пользователи/день | Логи авторизации | Ежедневно |
| Время генерации | Таймстампы запросов | Real-time |
| Success rate | Логи ошибок | Real-time |
| Распределение по категориям | Анализ промптов | Ежедневно |
| Retention (возвращаемость) | Сессии пользователей | Еженедельно |

**Качественные метрики (собираются через опросы):**

| Метрика | Инструмент | Частота |
|---------|------------|---------|
| NPS (Net Promoter Score) | Опрос | Ежемесячно |
| Satisfaction Score (1-5) | Встроенная оценка | После каждой генерации |
| Ease of Use (1-5) | Опрос | Ежемесячно |
| Time Savings (субъективно) | Опрос | Ежемесячно |
| Качественная обратная связь | Интервью | Ежемесячно |

---

### 3.2. Критерии успешного пилота

#### Обязательные критерии (Go/No-Go)

| Категория | Метрика | Целевое значение | Вес |
|-----------|---------|------------------|-----|
| **Adoption** | Регулярное использование (≥1 раз/неделю) | ≥ 60% пользователей | 25% |
| **Quality** | Соответствие бренд-буку | ≥ 70% изображений | 20% |
| **Quality** | Satisfaction Score | > 3.5/5 | 15% |
| **Efficiency** | Сокращение времени создания | ≥ 70% | 20% |
| **Satisfaction** | NPS | ≥ 40 | 10% |
| **Stability** | Success rate генерации | > 95% | 10% |

#### Формула успешности пилота

```
Pilot_Success = (Adoption ≥ 60%) AND (Quality ≥ 70%) AND (NPS ≥ 40) AND (TimeSaving ≥ 70%)
```

---

### 3.3. Подготовка пилота

#### Вычислительные ресурсы

**Текущая инфраструктура:**

| Ресурс | Характеристики | Количество |
|--------|----------------|------------|
| GPU | NVIDIA A40 (48GB VRAM) | 2 сервера |
| CPU | 32 cores | На каждом сервере |
| RAM | 256 GB | На каждом сервере |
| Storage | 2 TB NVMe | Для моделей и результатов |

**Расчёт пропускной способности:**

| Параметр | Значение |
|----------|----------|
| Время генерации 1024x1024 | ~1 минута |
| Генераций в час (1 GPU) | ~50 |
| Генераций в час (2 GPU) | ~100 |
| Пиковая нагрузка пилота | 10-15 генераций/час |
| **Запас мощности** | **6-10x** |

---

## 4. Внедрение

### 4.1. Архитектура решения

#### Диаграмма архитектуры технического решения

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                           PRODUCTION ARCHITECTURE                                       │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                         │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐│
│  │                              PRESENTATION LAYER                                     ││
│  │  ┌─────────────┐                                                                    ││
│  │  │   Browser   │  ←── SSO Auth (Corporate LDAP)                                     ││
│  │  │  (Web UI)   │                                                                    ││
│  │  │  React/Vue  │                                                                    ││
│  │  └──────┬──────┘                                                                    ││
│  │         │ HTTPS                                                                     ││
│  └─────────┼───────────────────────────────────────────────────────────────────────────┘│
│            │                                                                            │
│            ▼                                                                            │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐│
│  │                              API GATEWAY LAYER                                      ││
│  │  ┌─────────────────────────────────────────────────────────────────────────────┐    ││
│  │  │                         FastAPI Application                                 │    ││
│  │  │                                                                             │    ││
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │    ││
│  │  │  │ POST        │  │ GET         │  │ POST        │  │ GET         │         │    ││
│  │  │  │ /generate   │  │ /status/{id}│  │ /rate       │  │ /health     │         │    ││
│  │  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘         │    ││
│  │  │                                                                             │    ││
│  │  │  ┌─────────────────────────────────────────────────────────────────────┐    │    ││
│  │  │  │                    Request Validation & Auth                        │    │    ││
│  │  │  │  • JWT Token validation                                             │    │    ││
│  │  │  │  • Input sanitization                                               │    │    ││
│  │  │  │  • Rate limiting (10 req/min per user)                              │    │    ││
│  │  │  └─────────────────────────────────────────────────────────────────────┘    │    ││
│  │  └─────────────────────────────────────────────────────────────────────────────┘    ││
│  └─────────────────────────────────────────────────────────────────────────────────────┘│
│                                           │                                             │
│                                           ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐│
│  │                              PROCESSING LAYER                                       ││
│  │                                                                                     ││
│  │  ┌─────────────────────────────┐    ┌─────────────────────────────┐                 ││
│  │  │     LLM Enrichment Service  │    │     FLUX Generation Service │                 ││
│  │  │  ┌───────────────────────┐  │    │  ┌───────────────────────┐  │                 ││
│  │  │  │   DeepSeek-R1-671B    │  │    │  │      FLUX.1-dev       │  │                 ││
│  │  │  │   (Prompt Enrichment) │  │    │  │   (Image Generation)  │  │                 ││
│  │  │  └───────────────────────┘  │    │  └───────────────────────┘  │                 ││
│  │  │  ┌───────────────────────┐  │    │  ┌───────────────────────┐  │                 ││
│  │  │  │    Qwen2.5-14B        │  │    │  │    T5-XXL Encoder     │  │                 ││
│  │  │  │   (Translation/Neg)   │  │    │  │    CLIP-L Encoder     │  │                 ││
│  │  │  └───────────────────────┘  │    │  │    VAE Decoder        │  │                 ││
│  │  │                             │    │  └───────────────────────┘  │                 ││
│  │  │  Latency: ~2-3 sec          │    │  Latency: ~50-60 sec        │                 ││
│  │  └─────────────────────────────┘    └─────────────────────────────┘                 ││
│  │                                                                                     ││
│  └─────────────────────────────────────────────────────────────────────────────────────┘│
│                                           │                                             │
│                                           ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────────────────────┐│
│  │                              INFRASTRUCTURE LAYER                                   ││
│  │                                                                                     ││
│  │  ┌───────────────────────────┐  ┌───────────────────────────┐                       ││
│  │  │     GPU Server #1         │  │     GPU Server #2         │                       ││
│  │  │  ┌─────────────────────┐  │  │  ┌─────────────────────┐  │                       ││
│  │  │  │  NVIDIA A40 (48GB)  │  │  │  │  NVIDIA A40 (48GB)  │  │                       ││
│  │  │  │  • FLUX.1-dev       │  │  │  │  • FLUX.1-dev       │  │                       ││
│  │  │  │  • LLM Services     │  │  │  │  • LLM Services     │  │                       ││
│  │  │  └─────────────────────┘  │  │  └─────────────────────┘  │                       ││
│  │  │  CPU: 32 cores            │  │  CPU: 32 cores            │                       ││
│  │  │  RAM: 256 GB              │  │  RAM: 256 GB              │                       ││
│  │  │  NVMe: 1 TB               │  │  NVMe: 1 TB               │                       ││
│  │  └───────────────────────────┘  └───────────────────────────┘                       ││
│  │                                                                                     ││
│  │  ┌───────────────────────────────────────────────────────────────────────────────┐  ││
│  │  │                            Storage & Monitoring                               │  ││
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │  ││
│  │  │  │  PostgreSQL │  │ MinIO/S3    │  │  Prometheus │  │  Grafana    │           │  ││
│  │  │  │  (Metadata) │  │ (Images)    │  │ (Metrics)   │  │ (Dashboard) │           │  ││
│  │  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘           │  ││
│  │  └───────────────────────────────────────────────────────────────────────────────┘  ││
│  └─────────────────────────────────────────────────────────────────────────────────────┘│
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

#### Описание компонентов

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Web UI** | React/Vue.js | Пользовательский интерфейс для ввода промптов и получения результатов |
| **API Gateway** | FastAPI | REST API endpoints, валидация, аутентификация, rate limiting |
| **LLM Service** | DeepSeek-R1 + Qwen2.5 | Обогащение промптов, перевод, генерация negative prompts |
| **FLUX Service** | FLUX.1-dev + T5 + CLIP | Генерация изображений по обогащённым промптам |
| **PostgreSQL** | PostgreSQL 15 | Метаданные: пользователи, запросы, оценки |
| **Object Storage** | MinIO (S3-compatible) | Хранение сгенерированных изображений |
| **Monitoring** | Prometheus + Grafana | Метрики, алерты, дашборды |

#### Методы API

| Endpoint | Метод | Описание | Request | Response |
|----------|-------|----------|---------|----------|
| `/generate` | POST | Генерация изображения | `{prompt: str, height: int, width: int}` | `{task_id: str, status: str}` |
| `/status/{task_id}` | GET | Статус генерации | - | `{status: str, progress: float, image_url?: str}` |
| `/rate` | POST | Оценка результата | `{image_id: str, score: int, comment?: str}` | `{success: bool}` |
| `/health` | GET | Health check | - | `{status: "healthy", gpu_available: bool}` |

---

### 4.2. Описание инфраструктуры и масштабируемости

#### Выбранная инфраструктура

**On-premise решение на GPU-серверах компании.**

| Параметр | Значение | Обоснование |
|----------|----------|-------------|
| Тип развёртывания | On-premise | Требования ИБ, данные не покидают контур |
| GPU | 2x NVIDIA A40 (48GB) | Достаточно для FLUX.1-dev + LLM |
| CPU | 64 cores (2x32) | Параллельная обработка запросов |
| RAM | 512 GB (2x256) | LLM + кэширование |
| Storage | 2 TB NVMe + 10 TB HDD | Модели на NVMe, изображения на HDD |

#### Плюсы и минусы выбора

| Плюсы | Минусы |
|-------|--------|
| ✅ Полный контроль над данными | ❌ Ограниченная масштабируемость |
| ✅ Нет ежемесячных cloud-затрат | ❌ Требует DevOps-поддержки |
| ✅ Предсказуемая latency | ❌ CAPEX вместо OPEX |
| ✅ Compliance с ИБ-политиками | ❌ Нет auto-scaling |

#### Почему on-premise лучше cloud для данного проекта

1. **Требования безопасности**: Данные и промпты содержат информацию о бренд-буке и внутренних процессах
2. **Предсказуемые затраты**: При 100+ генераций/день cloud-решение обойдётся дороже
3. **Имеющиеся ресурсы**: GPU-серверы уже доступны в инфраструктуре
4. **Нагрузка пилота**: 10-15 генераций/час не требуют auto-scaling

#### Масштабирование

**Вертикальное (текущий подход):**
- Добавление GPU на существующие серверы (до 4x A40)
- Upgrade RAM при необходимости
- Оптимизация кода (batching, кэширование)

**Горизонтальное (post-MVP):**
- Kubernetes cluster с GPU-нодами
- Load balancer для распределения запросов
- Shared storage для моделей (NFS/Ceph)

---

### 4.3. Требования к работе системы

#### SLA (Service Level Agreement)

| Метрика | Целевое значение | Критическое |
|---------|------------------|-------------|
| **Availability** | 99% | < 95% |
| **Response Time (API)** | < 200ms | > 500ms |
| **Generation Time (1024x1024)** | < 90 sec | > 240 sec |
| **Error Rate** | < 2% | > 5% |

#### Пропускная способность

| Параметр | Значение |
|----------|----------|
| Concurrent users | 10-15 |
| Peak requests/hour | 50 |
| Avg requests/hour | 15-20 |
| Images generated/day | ~200-400 |

#### Задержка (Latency breakdown)

| Этап | Время | % от общего |
|------|-------|-------------|
| API validation | ~10ms | 0.02% |
| LLM Enrichment | ~2-3 sec | 3-5% |
| T5 + CLIP Encoding | ~2-3 sec | 3-5% |
| FLUX Diffusion (28 steps) | ~50-55 sec | 85-90% |
| VAE Decoding + Save | ~2-3 sec | 3-5% |
| **Total** | **~60-65 sec** | 100% |

---

### 4.4. Безопасность системы

#### Потенциальные уязвимости и митигация

| Уязвимость | Риск | Митигация |
|------------|------|-----------|
| **Prompt Injection** | Средний | Санитизация ввода, whitelist категорий |
| **SSRF через изображения** | Низкий | Валидация output, no external URLs |
| **DoS через тяжёлые запросы** | Средний | Rate limiting, queue с таймаутами |
| **Несанкционированный доступ** | Высокий | SSO, RBAC, audit logs |
| **Утечка модели** | Низкий | Network isolation, no model export |

#### Реализованные меры безопасности

- **Аутентификация**: Corporate SSO (LDAP/SAML)
- **Авторизация**: Role-Based Access Control
- **Шифрование**: TLS 1.3 для всех соединений
- **Аудит**: Логирование всех запросов с user_id
- **Изоляция**: GPU-серверы в отдельном VLAN

---

### 4.5. Безопасность данных

#### Соответствие GDPR и другим законам

| Аспект | Статус | Комментарий |
|--------|--------|-------------|
| **Персональные данные** | Минимальны | Только user_id (псевдонимизированный) |
| **Право на удаление** | Реализовано | Endpoint `/user/delete` удаляет все данные |
| **Срок хранения** | 6 месяцев | Автоматическая очистка старых генераций |
| **Передача третьим лицам** | Нет | Все данные внутри контура |
| **Cookie consent** | Не требуется | Внутренний сервис, корпоративная auth |

#### Классификация данных

| Тип данных | Классификация | Хранение | Retention |
|------------|---------------|----------|-----------|
| Промпты | Internal | PostgreSQL | 6 месяцев |
| Изображения | Internal | MinIO | 6 месяцев |
| User ID | Confidential | PostgreSQL | 6 месяцев |
| Логи | Internal | ELK Stack | 90 дней |
| Модели | Confidential | NVMe (local) | Permanent |

---

### 4.6. Издержки

#### Расчёт издержек на работу системы

**Инфраструктурные затраты (ежемесячно):**

| Компонент | Стоимость | Примечание |
|-----------|-----------|------------|
| GPU-серверы (амортизация) | ~$500/мес | 2x сервера, амортизация 3 года |
| Электроэнергия | ~$200/мес | ~2 kW на сервер, 24/7 |
| Охлаждение | ~$100/мес | Доля от общих затрат ЦОД |
| Storage | ~$50/мес | 10 TB HDD |
| Network | ~$50/мес | Internal traffic |
| **Итого инфраструктура** | **~$900/мес** | |

**Операционные затраты:**

| Компонент | Стоимость | Примечание |
|-----------|-----------|------------|
| DevOps support | ~$200/мес | 0.1 FTE |
| Monitoring/alerts | ~$50/мес | Grafana Cloud (optional) |
| **Итого операционные** | **~$250/мес** | |

**Общие издержки: ~$1,150/мес**

**Стоимость одной генерации:**
- При 10,000 генераций/мес: ~$0.11/генерация
- При 5,000 генераций/мес: ~$0.23/генерация

**Сравнение с альтернативами:**
- DALL-E 3 API: ~$0.04/изображение (но данные уходят в OpenAI)
- Midjourney: ~$0.02/изображение (но нет корпоративного стиля)
- Ручная работа: ~$20-50/иконка (2-3 часа дизайнера)

---

### 4.7. Integration Points

#### Взаимодействие между сервисами

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           INTEGRATION FLOW                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Client (Browser)                                                           │
│       │                                                                     │
│       │ POST /generate {prompt: "иконка машины", height: 1024}              │
│       ▼                                                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ FastAPI Gateway                                                     │    │
│  │   • Validate JWT token                                              │    │
│  │   • Sanitize input                                                  │    │
│  │   • Check rate limits                                               │    │
│  │   • Create task in PostgreSQL                                       │    │
│  │   • Return task_id                                                  │    │
│  └───────────────────────────────────┬─────────────────────────────────┘    │
│                                      │                                      │
│       ┌──────────────────────────────┴───────────────────┐                  │
│       ▼                                                  ▼                  │
│  ┌─────────────────────┐      (async)      ┌────────────────────────┐       │
│  │ Response to client  │                   │ Background Worker      │       │
│  │ {task_id: "abc123"} │                   │                        │       │
│  └─────────────────────┘                   │  1. Call LLM Service   │       │
│                                            │     POST /enrich       │       │
│       │                                    │     {prompt, colors}   │       │
│       │ GET /status/abc123                 │     → enriched_prompt  │       │
│       ▼                                    │                        │       │
│  ┌─────────────────────┐                   │  2. Call FLUX Service  │       │
│  │ Status: processing  │                   │     POST /generate     │       │
│  │ progress: 0.45      │                   │     {t5, clip, neg}    │       │
│  └─────────────────────┘                   │     → image bytes      │       │
│                                            │                        │       │
│       │                                    │  3. Save to MinIO      │       │
│       │ GET /status/abc123                 │     PUT /images/abc123 │       │
│       ▼                                    │                        │       │
│  ┌─────────────────────┐                   │  4. Update PostgreSQL  │       │
│  │ Status: completed   │                   │     status=completed   │       │
│  │ image_url: /img/... │◀─────────────────│     image_url=...      │       │ 
│  └─────────────────────┘                   └────────────────────────┘       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### API Contracts

**1. LLM Enrichment Service**

```python
# POST /enrich
Request:
{
    "prompt": str,              # Пользовательский промпт
    "corporate_colors": list,   # HEX-коды корпоративных цветов
    "style_hints": list         # Опциональные подсказки стиля
}

Response:
{
    "prompt_t5": str,           # Детальный промпт для T5
    "prompt_clip": str,         # Короткий промпт для CLIP
    "negative_prompt": str,     # Negative prompt
    "detected_category": str    # Автоопределённая категория
}
```

**2. FLUX Generation Service**

```python
# POST /generate
Request:
{
    "prompt": str,              # prompt для T5 (детальный)
    "prompt_2": str,            # prompt для CLIP (короткий)
    "negative_prompt": str,
    "height": int,              # Default: 1024
    "width": int,               # Default: 1024
    "guidance_scale": float,    # Default: 3.5
    "num_inference_steps": int, # Default: 28
    "seed": int                 # Optional, для воспроизводимости
}

Response:
{
    "image": bytes,             # PNG image data
    "metadata": {
        "generation_time_ms": int,
        "actual_seed": int
    }
}
```

---

### 4.8. Риски

#### Риски и неопределённости

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| **GPU OOM при высокой нагрузке** | Средняя | Высокое | Queue с FIFO, ограничение concurrent requests до 3 |
| **LLM галлюцинации** | Средняя | Среднее | Строгие system prompts, валидация output |
| **Деградация качества генерации** | Низкая | Среднее | Ежедневный мониторинг CLIP Score |
| **Отказ GPU-сервера** | Низкая | Высокое | Failover на второй сервер |
| **Медленный adoption пользователями** | Средняя | Среднее | Обучение, геймификация, поддержка |
| **Изменение требований бренд-бука** | Средняя | Низкое | Параметризованные system prompts |
| **Блокировка HuggingFace/моделей** | Низкая | Высокое | Локальное зеркало всех моделей |

#### Мониторинг и алерты

| Метрика | Порог Warning | Порог Critical | Действие |
|---------|---------------|----------------|----------|
| GPU Memory | > 80% | > 90% | Throttle requests |
| Generation Time | > 90 sec | > 180 sec | Investigate, reduce queue |
| Error Rate | > 2% | > 5% | Page on-call |
| Queue Length | > 10 | > 20 | Scale notice |
| CLIP Score (avg) | < 0.25 | < 0.20 | Review prompts |

---

### 4.9. Нагрузочное тестирование

#### Методология тестирования

**Инструменты:**
- Locust для генерации нагрузки
- Prometheus + Grafana для сбора метрик
- Python scripts для валидации качества

**Сценарии тестирования:**

| Сценарий | Описание | Параметры |
|----------|----------|-----------|
| **Baseline** | Одиночные запросы | 1 user, 1 req/min |
| **Normal Load** | Типичная нагрузка пилота | 5 users, 2 req/min each |
| **Peak Load** | Максимальная ожидаемая | 10 users, 3 req/min each |
| **Stress Test** | За пределами нормы | 20 users, 5 req/min each |
| **Endurance** | Длительная работа | 5 users, 8 hours |

#### Результаты нагрузочного тестирования

**Baseline Test (1 user, последовательные запросы):**

| Метрика | Значение |
|---------|----------|
| Avg Response Time | 58.3 sec |
| P95 Response Time | 67.2 sec |
| P99 Response Time | 72.1 sec |
| Throughput | ~1 req/min |
| Success Rate | 100% |
| GPU Utilization | 75% |

```
Generation Time Distribution (n=50):
┌────────────────────────────────────────────────────────┐
│  < 50s  ████████████████████████████ 35%               │
│  50-60s ████████████████████████████████████████ 50%   │
│  60-70s ██████████ 12%                                 │
│  > 70s  ███ 3%                                         │
└────────────────────────────────────────────────────────┘
```

**Normal Load Test (5 concurrent users):**

| Метрика | Значение |
|---------|----------|
| Avg Response Time | 72.4 sec |
| P95 Response Time | 95.3 sec |
| P99 Response Time | 112.7 sec |
| Throughput | ~4.2 req/min |
| Success Rate | 98.5% |
| GPU Utilization | 92% |
| Queue Depth (avg) | 2.3 |

```
Response Time Over Time (5 users, 30 min):
Time (min)  │ Avg Response (sec)
────────────┼────────────────────
    0-5     │ ████████████████████████████████████████ 65
    5-10    │ ████████████████████████████████████████████ 72
   10-15    │ ██████████████████████████████████████████████ 75
   15-20    │ ████████████████████████████████████████████ 73
   20-25    │ ████████████████████████████████████████████ 72
   25-30    │ ████████████████████████████████████████████ 71
────────────┴────────────────────
```

**Peak Load Test (10 concurrent users):**

| Метрика | Значение |
|---------|----------|
| Avg Response Time | 142.8 sec |
| P95 Response Time | 198.5 sec |
| P99 Response Time | 245.3 sec |
| Throughput | ~3.8 req/min |
| Success Rate | 95.2% |
| GPU Utilization | 98% |
| Queue Depth (avg) | 6.7 |

```
Queue Depth vs Response Time:
Queue │ Response Time
──────┼─────────────────────────────────────────────
  1   │ ████████████████████ 60s
  2   │ ████████████████████████████ 85s
  3   │ ████████████████████████████████████ 105s
  4   │ ██████████████████████████████████████████ 125s
  5+  │ ████████████████████████████████████████████████ 150s+
──────┴─────────────────────────────────────────────
```

**Stress Test (20 concurrent users):**

| Метрика | Значение |
|---------|----------|
| Avg Response Time | 312.5 sec |
| Error Rate | 12.3% |
| OOM Events | 3 |
| GPU Utilization | 100% |
| Queue Depth (max) | 18 |

**Выводы:**
- ⚠️ Система не выдерживает > 10 concurrent users
- ⚠️ При queue > 5 время ответа превышает SLA (240 sec)
- ✅ Ограничение concurrent requests до 6 обеспечивает стабильность

**Endurance Test (5 users, 8 hours):**

| Час | Avg Response | Error Rate | Memory Leak |
|-----|--------------|------------|-------------|
| 1 | 71.2 sec | 1.2% | None |
| 2 | 72.5 sec | 0.8% | None |
| 4 | 73.1 sec | 1.5% | None |
| 6 | 74.8 sec | 1.1% | None |
| 8 | 75.2 sec | 1.3% | None |

```
Memory Usage Over Time (8 hours):
Hour  │ GPU Memory (GB)
──────┼────────────────────────────────────────────
  0   │ ████████████████████████████████████ 36.2
  2   │ ████████████████████████████████████ 36.5
  4   │ █████████████████████████████████████ 37.1
  6   │ █████████████████████████████████████ 37.3
  8   │ █████████████████████████████████████ 37.5
──────┴────────────────────────────────────────────
No significant memory leak detected (< 4% growth)
```

#### Рекомендации по результатам тестирования

| Проблема | Решение | Приоритет |
|----------|---------|-----------|
| Response time > 90s при queue > 3 | Ограничить concurrent до 3-4 на GPU | High |
| OOM при 20+ users | Rate limiting 10 req/min total | High |
| P99 > 240s при peak | Добавить timeout 180s с retry | Medium |
| GPU utilization 98% | Рассмотреть 3-й GPU при росте | Low |

---

## Заключение

Дизайн-документ описывает MVP генеративного сервиса для создания 3D-иконок в корпоративном стиле.

**Ключевые особенности:**
- Использование предобученных моделей (inference-only) без fine-tuning
- Интеграция корпоративных гайдлайнов через промпт-инженеринг
- Автоматическое обогащение промптов через LLM
- Dual prompting (T5 + CLIP) для оптимального качества

**Достигнутые результаты:**
- CLIP Score: 0.28 (целевой ≥0.25) ✅
- Usability: 75% (целевой ≥70%) ✅
- Экспертная оценка: 3.85/5 (целевой ≥3.5) ✅
- Время генерации: 58 сек (целевой <4 мин) ✅

**Результаты нагрузочного тестирования:**
- Baseline: 58 sec, 100% success rate ✅
- Normal (5 users): 72 sec, 98.5% success rate ✅
- Peak (10 users): 143 sec, 95.2% success rate ⚠️
- Рекомендация: ограничить concurrent до 6 запросов

**Следующие шаги:**
1. Запуск пилота (этап 1: 5-7 дизайнеров)
2. Сбор метрик и обратной связи
3. Go/No-Go решение на неделе 12
4. Масштабирование на всю команду (при успехе)
5. Развитие: LoRA fine-tuning, Figma-плагин, RAG

---
